# 语音绘图指令能力设计文档

记录日期：2026-06-14

本文档记录本项目计划支持的语音指令能力、当前已经实现的能力，以及暂未完成部分和原因。它作为 README 的补充，用于说明开发范围和后续迭代方向。

## 一、产品目标

项目目标是做一个纯语音驱动的绘画工具。用户面对一块白色画布，通过语音逐步描述想画的内容，系统把这些描述转成绘画操作或模型生成请求，并把每一步结果记录到右侧时间线。

核心体验目标：

- 用户只需要点击一个语音按钮即可开始说话。
- 语音识别内容要及时展示给用户。
- 开放式绘画内容由 Agnes 模型生成，而不是本地代码硬编码树、河流、苹果等图案。
- 整个画布从始至终是一幅作品，用户逐步补充元素，而不是把多张小图片叠在画布上。
- 清空、撤销、回到第几步等控制命令仍由本地引擎稳定执行。

## 二、整体架构

| 层级 | 模块 | 职责 |
|---|---|---|
| 语音采集 | `voice/audio_buffer.py` | 采集麦克风音频、VAD 静音检测、音量反馈 |
| 语音识别 | `voice/voice_service.py` | Whisper 识别、实时片段展示、低置信过滤 |
| 命令解析 | `parser/command_parser.py` | 解析基础几何、系统控制、颜色、编辑命令 |
| 智能规划 | `ai/drawing_agent.py` | 处理语义标签、局部补充、传统元素兜底规划 |
| 图像生成 | `ai/agnes_service.py` | Agnes 文生图、图生图、提示词风格约束、背景清理 |
| 执行引擎 | `engine/drawing_engine.py` | 操作历史、撤销/重做、整幅画布 I2I 状态、后台生成 |
| 画布渲染 | `ui/canvas_widget.py` | QPainter 渲染本地图元和整幅画布 AI 图片 |
| 用户界面 | `ui/main_window.py` | 语音按钮、画布、时间线、执行路由 |

## 三、计划支持的指令能力

### 1. 语音交互控制

计划支持：

- 开始语音识别
- 停止语音识别
- 实时显示正在识别的文字
- 展示麦克风音量和识别状态
- 识别失败或置信度不足时给出明确反馈

最终实现：

- 已实现语音按钮启动/停止识别。
- 已实现实时片段展示和最终识别文本展示。
- 已实现麦克风音量反馈。
- 已实现低置信识别提示和丢弃逻辑。
- 已实现 Esc 停止监听的备用快捷键。

### 2. 系统控制指令

计划支持：

- “清空画布”
- “撤销”
- “重做”
- “撤销到第二步”
- 点击右侧时间线回退作品步骤

最终实现：

- 已实现清空画布，并同步清空操作历史、时间线和整幅画布 I2I 状态。
- 已实现撤销和重做。
- 已实现“撤销到第 X 步 / 回到第 X 步”。
- 已实现右侧时间线快照记录和点击回退。

### 3. 基础绘图与编辑指令

计划支持：

- “画一个圆”
- “画一个矩形”
- “画一个三角形”
- “画一条线”
- “把它往右移一点”
- “把它变大一点”
- “改成红色”
- “删除这个图形”
- “我刚刚画的是一个太阳”

最终实现：

- 已实现圆、矩形、三角形、星形、线条等基础图形。
- 已实现最近图形选中状态和虚线选择框。
- 已实现移动、缩放、改色、填充、删除。
- 已实现语义标签，例如把最近图形记为“太阳”。
- 已实现太阳细节补充等本地可控细节绘制。

说明：

基础几何能力保留为本地引擎能力，主要用于稳定测试、兜底和简单编辑。开放式创作内容优先走整幅画布模型生成。

### 4. 开放式整幅画布创作

计划支持：

- 用户说“画一棵树”，模型在空白画布生成第一幅画。
- 用户继续说“树下面画一条小河”，模型基于上一张画布继续修改。
- 用户继续说“河里面画几条鱼”，模型保持原有画面不变，只补充鱼。
- 用户可以逐步丰富同一幅作品，而不是一次性给出完整提示词。

最终实现：

- 已实现首次整幅画布文生图。
- 已实现后续基于上一张画布的图生图。
- 已实现提示词累积，将历史步骤作为上下文传给模型。
- 已实现 `AIImageOperation(full_canvas=True)`，明确标记“这张图就是整幅画布状态”。
- 已实现整幅画布图片按画布状态渲染，不再作为小贴图裁剪叠加。
- 已实现撤销后恢复上一张 I2I 图片和提示词历史。
- 已实现右侧时间线记录每一轮整幅画布结果。

### 5. 模型生成风格控制

计划支持：

- 所有开放式生成保持单色简笔画风格。
- 背景保持原画布一样的纯白。
- 不生成照片感纸张、米色底纹、投影或方形贴图边框。

最终实现：

- 已在 Agnes 提示词中加入单色线稿、纯白数字画布、禁止纸张纹理等约束。
- 已实现生成后处理，把近白/米色纸张背景清理成纯白。
- 已实现黑色线稿保留，背景清理不擦除主要线条。

### 6. 响应速度与不卡顿

计划支持：

- 用户说完指令后，界面应立即反馈“正在生成”。
- Agnes 网络请求不能阻塞 PyQt 主线程。
- 用户等待生成时窗口不能显示“未响应”。

最终实现：

- 已将整幅画布 AI 生成切到后台线程。
- 已新增生成中状态 `is_canvas_i2i_busy()`。
- 已新增完成信号 `canvas_i2i_finished`，生成结束后再刷新时间线和提示语。
- 正在生成时重复触发会提示“AI 正在生成中，请稍等”。

## 四、最终实现的执行流

当前语音指令进入系统后按以下优先级处理：

```text
用户语音
  -> 开始/停止监听
  -> 回退到指定步骤
  -> 系统命令：清空 / 撤销 / 重做
  -> 整幅画布 AI 创作：树、河、苹果、鱼、风景等开放式内容
  -> DrawingAgent：语义标签、局部细节、传统智能规划
  -> CommandParser：基础几何、颜色、工具、编辑命令
```

开放式内容的核心流程：

```text
第一次创作：
  用户语音 -> 整理提示词 -> Agnes 文生图 -> 背景清理 -> full_canvas 操作入历史 -> 刷新画布

后续创作：
  用户语音 -> 读取上一张整幅画 -> 整理增量提示词 -> Agnes 图生图 -> 背景清理 -> 新 full_canvas 操作入历史 -> 刷新画布
```

## 五、已完成但仍有边界的能力

### 1. 语音识别

已完成：

- 可以听到麦克风输入并识别。
- 能展示识别文本和识别状态。
- 能过滤低置信或明显不稳定结果。

边界：

- Whisper 本地识别在嘈杂环境或短句中仍可能误识别。
- 中文同音词仍会导致“树/书”“河/盒”等错误。
- 当前主要靠规则纠错，尚未接入专门的语言模型纠错确认流程。

### 2. 整幅画布 I2I

已完成：

- 支持一幅画逐步生成。
- 支持撤销后继续从上一幅画生成。
- 支持背景清理和风格约束。

边界：

- Agnes 图生图仍可能改变原图细节，这是模型能力限制。
- 当前没有 mask/inpainting 局部遮罩，所以不能百分百保证“只改某个区域”。
- 如果 Agnes API 对图生图字段兼容性变化，需要更新 `extend_image()` 的请求字段。

### 3. 本地对象编辑

已完成：

- 对本地几何图形可以移动、缩放、改色、删除。

边界：

- 对整幅画布 AI 图片里的某个元素，例如“只移动树上的一个苹果”，当前无法精确编辑。
- 这需要图像分割、对象检测或模型局部编辑能力。

## 六、未完成部分与原因

### 1. 精确局部编辑

计划：

- “把树上的第一个苹果移到左边”
- “只把河流涂成蓝色”
- “把太阳擦掉”

未完成原因：

当前整幅画布是模型生成的一张图片，本地引擎不知道图片中每个元素的精确像素区域。要实现精确局部编辑，需要对象分割、遮罩编辑或模型 inpainting 支持。现有实现没有稳定的 mask 流程，因此暂未完成。

### 2. 多目标选择

计划：

- “选中蓝色圆形”
- “选中左上角那棵树”
- “选中所有苹果”

未完成原因：

本地几何对象可以按类型和颜色选择，但 AI 生成画布中的元素没有结构化对象 ID。要支持 AI 图里的多目标选择，需要在生成时同步维护元素元数据，或使用视觉模型识别元素位置。

### 3. 更强的智能纠错确认

计划：

- 识别不确定时询问：“你是要画一条小河吗？”
- 对“盒/河”“书/树”等高频误识别做上下文纠错。

未完成原因：

目前实现了部分规则纠错和低置信过滤，但没有完整的对话式确认流程。加入确认机制需要 UI 状态机：保存待确认指令、接收“是/不是”、再决定是否执行。

### 4. 画布视图控制

计划：

- “放大画布”
- “缩小画布”
- “移动视图到左边”
- “回到中心”

未完成原因：

早期已有缩放控件，但当前产品形态收敛为极简白布和语音按钮。视图控制需要重新设计滚动区域、缩放比例和语音命令之间的交互，暂未作为本轮核心功能。

### 5. 手动文本指令输入

计划：

- 增加一个文本输入框，用户可以不用语音，直接输入“画一棵树”执行。

未完成原因：

主流程聚焦纯语音体验，`MainWindow._on_execute_requested()` 已留有入口，但 UI 面板尚未放置文本输入组件。后续可以作为调试和无麦克风环境的辅助功能补上。

## 七、测试覆盖

本轮已加入或维护的重点测试：

| 测试文件 | 覆盖内容 |
|---|---|
| `tests/test_model_canvas_i2i.py` | 整幅画布首次生成、图生图、撤销恢复、后台生成不卡 UI |
| `tests/test_agnes_background_cleanup.py` | 米色/纸张背景清理为纯白、提示词禁止纸张纹理 |
| `tests/test_voice_integration.py` | 语音到画布、清空、删除、时间线、开放式生成后台执行 |
| `tests/test_voice_editing_operations.py` | 移动、缩放、改色、删除等对象编辑 |
| `tests/test_history_timeline_snapshots.py` | 右侧时间线快照 |
| `tests/test_canvas_rendering.py` | 画布渲染和选中框 |

提交前使用以下方式验证：

```powershell
$failed = @()
Get-ChildItem .\tests\test_*.py | Sort-Object Name | ForEach-Object {
    python $_.FullName
    if ($LASTEXITCODE -ne 0) { $failed += $_.Name }
}
if ($failed.Count -gt 0) { exit 1 }
```

## 八、第三方依赖与原创说明

第三方依赖：

- PyQt6：桌面 UI 和信号机制
- OpenAI Whisper：本地语音识别
- sounddevice / numpy / soundfile：麦克风采集和音频处理
- Pillow：图片背景清理
- requests：Agnes API HTTP 调用
- Agnes Image 2.1 Flash：文生图和图生图

原创功能：

- 语音绘图交互流程
- 命令解析和执行路由
- 整幅画布 I2I 状态管理
- 时间线快照和回退
- 后台生成防卡顿机制
- 生成提示词组织
- 纯白背景后处理
- 本地撤销、清空、对象编辑和兜底绘图逻辑

本设计文档未引用或复用个人历史项目代码片段。
