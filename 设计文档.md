# AI 语音绘图工具 - 设计文档

## 概述

纯语音控制的桌面绘图应用。用户只能通过语音指令进行所有操作——选择工具、选择颜色、绘制形状、撤销、保存、AI 生成图片等。整个系统需要高精度的语音识别、低延迟的响应、以及清晰的视觉反馈。

## 技术栈

| 组件 | 选择 | 说明 |
|---|---|---|
| 语言 | Python 3.10+ | 桌面应用开发 |
| GUI | PyQt6 + QPainter | 跨平台桌面框架，QPainter 原生绘图 |
| 语音识别 | OpenAI Whisper (base/small) | 本地模型，隐私安全 |
| AI 绘图 | OpenAI DALL·E 3 | 高质量文本到图像 |
| 麦克风采集 | sounddevice + numpy | 低延迟实时音频流 |

## 指令能力矩阵

### 已实现 (P0/P1)

| 指令 | 分类 | 示例 | 状态 |
|---|---|---|---|
| 笔/铅笔/画笔 | 工具选择 | "用画笔" | ✅ 已实现 |
| 橡皮/擦除 | 工具选择 | "用橡皮" | ✅ 已实现 |
| 线条 | 工具选择 | "用线条" | ✅ 已实现 |
| 颜色名（红/蓝/绿...50+） | 颜色选择 | "红色" / "深蓝" | ✅ 已实现 |
| 颜色 HEX | 颜色选择 | "颜色 #FF5733" | ✅ 已实现 |
| 粗/细/大小数字 | 笔刷粗细 | "粗的" / "大小 10" | ✅ 已实现 |
| 画圆/圆形 | 形状绘制 | "画个圆" | ✅ 已实现 |
| 画矩形/方形 | 形状绘制 | "画个矩形" | ✅ 已实现 |
| 画三角形 | 形状绘制 | "画个三角" | ✅ 已实现 |
| 画星/五角星 | 形状绘制 | "画个星" | ✅ 已实现 |
| 画线/直线 | 形状绘制 | "画一条线" | ✅ 已实现 |
| 中心/左上/右上等 | 定位 | "画在中心" | ✅ 已实现 |
| 撤销 | 操作 | "撤销" | ✅ 已实现 |
| 清空画布 | 操作 | "清空" | ✅ 已实现 |
| 保存 | 操作 | "保存" | ⚠️ 解析已实现，文件保存待补充 |
| 生成图片 | AI 生成 | "生成一幅日落海景" | ✅ 已实现 |
| 开始/停止监听 | 系统 | "开始监听" | ✅ 已实现 |
| 自由手绘 | 形状绘制 | "随便画" | ✅ 已实现（基础） |

### 未实现 (P2/后续)

| 指令 | 分类 | 原因 |
|---|---|---|
| 从 A 到 B 双点定位 | 定位 | 复杂指令拆解，需额外开发 |
| 重复上次 | 操作 | 功能简单，优先级低 |
| 保存为 SVG | 操作 | PNG 已满足基本需求，SVG 导出需额外库 |
| 图层管理 | 操作 | 单画布设计，图层增加复杂度 |
| 缩放/平移画布 | 操作 | 当前为固定画布，后续可加 |
| 快捷键（非紧急） | 系统 | 纯语音设计，键盘仅作紧急备用 |

## 文件结构

```
AI-voice-to-image/
├── main.py                          # 应用入口
├── config.py                        # 全局配置
├── requirements.txt                 # 依赖清单
├── README.md                        # 项目说明
├── 设计文档.md                      # 本文档
│
├── ui/
│   ├── canvas_widget.py             # QPainter 画布渲染
│   ├── voice_feedback_panel.py      # 语音识别反馈面板
│   ├── command_history_panel.py     # 命令历史面板
│   ├── status_bar.py                # 状态栏
│   └── main_window.py               # 主窗口组装
│
├── voice/
│   ├── audio_buffer.py              # 麦克风采集 + 环形缓冲区
│   └── voice_service.py             # Whisper 推理 + 双模型策略
│
├── parser/
│   ├── color_map.py                 # 30+ 颜色名 → HEX 映射
│   ├── command_grammar.py           # 预编译正则模式
│   └── command_parser.py            # 四层解析流水线
│
├── engine/
│   ├── operations.py                # 绘图操作数据模型
│   ├── undo_history.py              # 撤销/重做栈
│   └── drawing_engine.py            # 绘图执行引擎
│
├── ai/
│   ├── ai_service.py                # AI 服务抽象接口
│   └── dall_e_service.py            # DALL·E 3 实现
│
└── tests/
    └── (待补充)
```

## 关键技术设计

### 1. 语音识别延迟优化

| 阶段 | 目标延迟 | 实际 |
|---|---|---|
| 麦克风采集 | < 50ms | ✅ < 50ms |
| Whisper base 推理 | < 1s | ✅ ~1s (CPU) |
| Whisper small 回退 | < 2s | ✅ ~2s (CPU) |
| 命令解析 | < 5ms | ✅ < 1ms |
| 绘图执行 | < 10ms | ✅ < 1ms |
| 画布重绘 | < 16ms | ✅ ~5ms |
| **总计（最佳）** | **< 1.1s** | **~1.1s** |
| **总计（回退）** | **< 3.1s** | **~3.1s** |

### 2. 指令理解准确性

- **正则优先解析**：确定性 < 1ms，避免 LLM 解析的 2-5s 延迟
- **双模型策略**：base 先行 + 置信度回退 small，提升低置信度场景
- **VAD 噪音过滤**：RMS 音量阈值，静音时不触发 Whisper
- **50+ 颜色名覆盖**：中文 + 英文名，兼容不同语音识别结果

### 3. 撤销/重做

- **栈式双栈设计**：undo/redo 各自独立栈，O(1) 操作
- **仅存操作数据**：每个 ~100 字节，50 条上限约 5KB
- **远低于像素快照方案**：像素方案 1920×1080×3 字节 × 50 ≈ 300MB

### 4. 信号架构

```
麦克风 → AudioBuffer → VoiceService → CommandParser → DrawingEngine
                                                    ↓
                                             CanvasWidget
```

所有跨模块通信通过 PyQt Signal/Slot 实现，保证线程安全和解耦。

## 依赖清单

| 包 | 版本 | 用途 |
|---|---|---|
| pyqt6 | >= 6.6 | GUI 框架 |
| pyqt6-plugins | - | PyQt 插件（媒体等） |
| openai-whisper | >= 20231117 | 本地语音识别 |
| sounddevice | >= 0.4.6 | 麦克风音频采集 |
| numpy | >= 1.24 | 音频数据处理 |
| soundfile | >= 0.12 | 音频文件读写 |
| openai | >= 1.0 | DALL·E API 客户端 |
| requests | >= 2.31 | HTTP 请求（图片下载） |
| pydantic | >= 2.0 | 数据校验 |
| Pillow | >= 10.0 | 图像处理 |

## 环境变量

| 变量 | 说明 | 必填 |
|---|---|---|
| OPENAI_API_KEY | DALL·E API 密钥 | 仅 AI 生成需要 |

## 限制与已知问题

1. **Whisper 在 CPU 上较慢**：base 模型 ~1s/10s 音频，small 模型 ~2s
2. **Whisper 中文置信度不可直接获取**：使用启发式估算（token logprobs + 文本长度）
3. **AI 生成有成本**：DALL·E 3 每次约 $0.04，缓存减少重复开销
4. **无鼠标/键盘操作**：纯语音设计，紧急情况下需键盘快捷键（Esc/Ctrl+Z）
5. **测试覆盖率低**：目前无自动化测试，需手动联调

## 开发记录

| PR | 文件 | 描述 |
|---|---|---|
| PR 0 | config.py, README.md 等 | 项目初始化 |
| PR 1 | engine/operations.py | 绘图操作数据模型 |
| PR 2 | engine/undo_history.py | 撤销/重做历史 |
| PR 3 | engine/drawing_engine.py | 绘图执行引擎 |
| PR 4 | ui/canvas_widget.py | 画布渲染 |
| PR 5 | parser/color_map.py | 颜色映射表 |
| PR 6 | parser/command_grammar.py | 命令语法规则 |
| PR 7 | parser/command_parser.py | 命令解析器 |
| PR 8 | voice/audio_buffer.py | 音频缓冲区 |
| PR 9 | voice/voice_service.py | 语音识别服务 |
| PR 10 | ui/status_bar.py | 状态栏 |
| PR 11 | ui/voice_feedback_panel.py | 语音反馈面板 |
| PR 12 | ui/command_history_panel.py | 命令历史面板 |
| PR 13 | ui/main_window.py | 主窗口组装 |
| PR 14 | ai/ai_service.py | AI 服务接口 |
| PR 15 | ai/dall_e_service.py | DALL·E 实现 |
| PR 16 | main.py | 应用入口 |
